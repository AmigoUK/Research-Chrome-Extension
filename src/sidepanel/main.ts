/**
 * Side panel entry point.
 *
 * Scaffold only — the full companion UI (capture card, reading list,
 * citation actions) is ported from the design prototype in milestone M5.
 */
const app = document.getElementById('app');
if (app) {
  app.dataset.ready = 'true';
}

export {};
