/**
 * Options / dashboard entry point.
 *
 * Scaffold only — the full dashboard (Kanban, references, style editor)
 * is built in Phase 2.
 */
const dashboard = document.getElementById('dashboard');
if (dashboard) {
  dashboard.dataset.ready = 'true';
}

export {};
