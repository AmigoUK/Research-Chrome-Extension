# Dependencies
node_modules/

# Build output
dist/
release/
*.crx
*.zip

# Test artifacts
coverage/
playwright-report/
test-results/
.vitest/

# Vite
.vite/

# Editor / OS
.DS_Store
._*
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# Env
.env
.env.local

# Runtime lock held by the local tooling, committed by accident in v0.18.0. It
# is machine state, not source: tracked, it leaves every checkout permanently
# dirty as the tooling takes and releases it.
.claude/*.lock
