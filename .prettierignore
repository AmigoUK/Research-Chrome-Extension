# Build output — never hand-edited, so never hand-formatted.
dist/
node_modules/
coverage/
playwright-report/
test-results/

# Generated by `npm run pages` from doc/PRIVACY.md. Prettier and the generator
# would fight over it: reformat it here and the next `npm run pages` undoes the
# change, turning CI red on an unrelated docs edit.
docs/

# Prose, hand-wrapped to read well in a terminal. Reflowing it is not an
# improvement, and doc/design_mock/ is a read-only design contract (CLAUDE.md).
doc/

# Local process artifacts from the subagent-driven-development skill. Not in
# git, so not in a CI checkout either.
.superpowers/
